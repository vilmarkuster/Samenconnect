/**
 * Single Claude model ID for all platform routes that call Anthropic
 * (`/api/chat`, `/api/agents/run`). Import this instead of defining a local constant.
 */
export const PLATFORM_ANTHROPIC_CLAUDE_MODEL = "claude-sonnet-4-6";
